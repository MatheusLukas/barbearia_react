import { NewDialog } from "../components/NewDialog";

export default function Test() {
  return (
    <main className="flex w-full min-h-screen items-center justify-center bg-zinc-900 text-white">
      <NewDialog />
    </main>
  );
}
